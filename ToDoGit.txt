1) Im Terminal in Verzeichnis wechseln: ~/workspace/ModHPS
	git branch (zeigt den branch an, in dem man gerade ist)
	git checkout development (in den passenden branch wechseln -> hier development)
2) git add --all

3) git commit --all -m "I changed ...." (in regelmäßigen Abständen comitten (auch ohne pushs))

4) git push

(git push -u origin development/issue1	(issues an andere; ich selber arbeite im development branch; den Branch in dem ich gerade arbeite -> fast nie den master (nur wenn ich etwas veröffentlichen will! -> GitLab und automatisch GitHub durch mirrorn))

5) Username: weck

(https://weck@gitlab.e3d.rwth-aachen.de)
